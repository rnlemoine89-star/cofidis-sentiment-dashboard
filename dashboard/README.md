# Cofidis Sentiment Dashboard

Open [index.html](./index.html) in a browser to view the dashboard.

If the workbook changes, regenerate the embedded dashboard data with:

```bash
python3 "./generate_dashboard_data.py"
```

The generator reads `../Sentiment Analysis Masterfile-2.xlsx` and rewrites `data.js`.
